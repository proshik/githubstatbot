# .goreleaser.yml
# Build customization
builds:
  - main: main.go
    binary: githubstatbot
    goos:
      - windows
      - darwin
      - linux
    goarch:
      - amd64
# Archive customization
archive:
  format: tar